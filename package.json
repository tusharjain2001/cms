{
  "name": "pagecraft",
  "version": "0.1.0",
  "private": true,
  "description": "Headless CMS for client websites built in React / Next.js",
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ],
  "scripts": {
    "shared": "npm run build --workspace @pagecraft/shared && npm run build --workspace @pagecraft/sdk",
    "dev:api": "npm run shared && npm run dev --workspace @pagecraft/api",
    "dev:admin": "npm run dev --workspace @pagecraft/admin",
    "build": "npm run shared && npm run build --workspace @pagecraft/api && npm run build --workspace @pagecraft/admin",
    "test": "npm run shared && npm run test --workspaces --if-present",
    "typecheck": "npm run shared && npm run typecheck --workspaces --if-present",
    "seed": "npm run shared && npm run seed --workspace @pagecraft/api",
    "dev:demo": "npm run shared && npm run dev --workspace @pagecraft/demo-site",
    "build:demo": "npm run shared && npm run build --workspace @pagecraft/demo-site"
  }
}
